{
    my $admins = $ejabberd{'Admins'} || '^admin$';
    if ($admins ne "admin")
    {
	my @tmp = split(/,/,$admins);
	$admins = join('$|^',@tmp);
        $admins = "^$admins\$";
    } 
    $OUT .= "%Admin users\n";
    $OUT .= "{acl, admin, {user_regexp, \"$admins\"}}.";
}

